## C++ version of Snake with some OOP

### Dependencies 
#### Uses SFML for graphics and keyboard input
$ sudo apt-get install libsfml-dev 

### Compile 
$ cd /path/to/snake
$ mkdir build && cd build 
$ cmake .. && make -j12